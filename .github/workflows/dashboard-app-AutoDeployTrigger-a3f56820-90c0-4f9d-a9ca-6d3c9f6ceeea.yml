name: Trigger auto deployment for dashboard-app

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '**'
    - '.github/workflows/dashboard-app-AutoDeployTrigger-a3f56820-90c0-4f9d-a9ca-6d3c9f6ceeea.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v2
        with:
          client-id: ${{ secrets.DASHBOARDAPP_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.DASHBOARDAPP_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.DASHBOARDAPP_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}
          _dockerfilePathKey_: _dockerfilePath_
          _targetLabelKey_: _targetLabel_
          registryUrl: 
          registryUsername: ${{ secrets.DASHBOARDAPP_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.DASHBOARDAPP_REGISTRY_PASSWORD }}
          containerAppName: dashboard-app
          resourceGroup: rg-dashboardd
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


